请基于我提供的当前代码，对“审计日志”页面做一次设计改造。不要重写整体页面，只在现有结构上调整。

本次改造重点不是新增复杂功能，而是优化页面的信息组织、筛选维度和表格字段顺序，使页面更符合审计日志查看场景。

一、总体设计方向

这个页面用于查看 Kubernetes / 云平台场景下的审计日志，目标用户包括平台管理员、运维人员、安全审计人员。

页面需要表达的是：

1. 谁在什么时间做了什么操作；
2. 操作作用于什么资源；
3. 操作发生在哪个命名空间；
4. 操作是否成功；
5. 是否需要进一步查看详情。

整体设计要偏后台管理系统风格，清晰、克制、专业，不要做成过于花哨的展示页。

二、页面信息层级

页面整体保留三个层次：

1. 顶部标题区域

用于说明当前页面是“审计日志”，并用一句简短描述说明页面用途。

建议表达为：
- 标题：审计日志
- 描述：查看平台用户、人工运维、系统组件在集群中的操作记录。

2. 快速筛选区域

快速筛选区域用于让用户快速切换常见审计视角。

保留以下筛选项：
- 全部
- 平台用户
- 人工运维
- 系统组件
- 重点操作
- 失败操作

请移除以下两个大页签或筛选项：
- 全县相关
- 密钥相关

这里不需要强调某一类 Kubernetes 资源，而是从审计视角出发，按操作者类型、操作重要性、操作结果进行快速筛选。

3. 操作记录区域

操作记录区域是页面核心，用表格展示审计日志。

表格上方可以保留现有筛选控件，例如：
- 时间范围
- 命名空间
- 操作类型
- 关键词搜索

这些筛选控件的目标是帮助用户进一步缩小日志范围。

三、表格字段调整

请重点调整表格字段顺序。

最终表格列顺序必须是：

1. 操作者
2. 操作
3. 资源
4. 结果
5. 命名空间
6. 详情
7. 时间

注意：

1. 移除“来源”列。
   审计场景中，“来源”信息对当前页面不是一级信息，可以放到详情里，不需要在主表中展示。

2. 将“结果”列放到“命名空间”列之前。
   用户查看审计日志时，通常会先判断操作是否成功，再进一步关注发生在哪个命名空间。

3. 将“时间”列放到最后。
   时间是日志排序和定位的重要信息，但在当前表格阅读路径中放在最后更符合这次设计要求。

四、各字段的设计含义

1. 操作者

展示执行操作的人或系统身份。

操作者可以来自：
- 平台用户
- 人工运维
- 系统组件

建议在展示上能区分操作者名称和操作者类型，但不强制要求具体样式。

2. 操作

展示 Kubernetes 审计日志中的操作动作，例如：
- create
- update
- patch
- delete
- get
- list

建议视觉上突出不同操作类型，尤其是 delete、patch、update 这类更需要关注的操作。

3. 资源

展示被操作的资源对象。

建议能同时表达：
- 资源类型
- 资源名称

如果现有代码中已经有资源版本、资源组等信息，可以继续作为次要信息展示，但不要让它喧宾夺主。

4. 结果

展示本次操作是否成功。

结果字段应当被放在较靠前的位置，并在视觉上容易识别。

建议区分：
- 成功
- 失败

失败操作需要更容易被注意到。

5. 命名空间

展示资源所在命名空间。

如果是集群级资源，没有命名空间，可以显示为：
- -
- 集群级
- 无命名空间

具体沿用现有系统表达即可。

6. 详情

用于查看单条审计日志的完整内容。

主表中不再展示过多辅助信息，辅助信息可以通过详情查看。

7. 时间

展示操作发生时间。

时间列放到最后。

五、快速筛选项的设计含义

请根据以下含义调整快速筛选区域：

1. 全部

展示当前条件下的全部审计日志。

2. 平台用户

展示由平台普通用户或平台账号发起的操作。

3. 人工运维

展示由运维人员、管理员、审计账号等人工介入产生的操作。

4. 系统组件

展示由 Kubernetes 或平台内部组件产生的操作，例如 scheduler、controller-manager、service-account 等。

5. 重点操作

展示需要重点关注的操作。

重点操作可以根据系统现有逻辑判断，例如：
- 删除资源
- 修改权限
- 修改工作负载
- 修改准入策略
- 修改网络策略
- 其他高风险变更

这里不要求前端写死判断规则，建议由后端或现有数据逻辑提供分类结果。

6. 失败操作

展示执行失败的审计日志，用于排查权限、准入、资源冲突等问题。

六、接口设计建议

我认为这个页面可以保留或调整为两个后端接口。

第一个接口：审计日志统计接口

用途：
用于获取快速筛选区域中各筛选项对应的数量。

这个接口服务于页面顶部的快速筛选卡片或按钮，让用户能够先看到不同审计视角下的日志数量分布。

统计维度包括：
- 全部
- 平台用户
- 人工运维
- 系统组件
- 重点操作
- 失败操作

统计结果应该受当前基础筛选条件影响，例如时间范围、命名空间、关键词等。

第二个接口：审计日志列表接口

用途：
用于获取主表格中的审计日志列表。

列表接口需要支持：
- 快速筛选项
- 时间范围
- 命名空间
- 操作类型
- 关键词
- 分页
- 排序

列表接口返回的数据应满足主表展示所需的信息：

主表只需要展示：
- 操作者
- 操作
- 资源
- 结果
- 命名空间
- 详情入口
- 时间

其他辅助字段，例如来源、请求 URI、User-Agent、IP、审计阶段、requestObject、responseObject 等，不建议在主表中直接展示，可以放在详情中。

七、后端实现关注点

因为我是后端开发，所以请重点注意以下产品含义，而不是只改前端样式：

1. “来源”不是主表一级字段

来源信息可以继续保留在数据中，但不要作为主表列展示。
如果现有系统需要展示来源，请放到详情中。

2. “重点操作”应由后端或统一规则计算

不要让页面简单用文字匹配临时判断。
重点操作建议由后端根据资源类型、操作类型、权限影响范围等统一判断。

3. 操作者类型需要有稳定分类

建议后端能够区分：
- 平台用户
- 人工运维
- 系统组件

如果当前日志中只有 Kubernetes username、groups、userAgent 等原始字段，可以在后端加工成更适合页面展示的分类。

4. 结果字段需要统一口径

建议后端统一处理成功 / 失败状态，不要让页面根据不同字段临时判断。

5. 命名空间为空时要有统一表达

集群级资源或非命名空间资源，需要有统一展示口径。

6. 详情中可以保留完整审计信息

主表保持简洁，但详情中可以展示完整原始日志，便于排查和审计取证。

八、本次改造的验收标准

请按以下标准修改当前代码：

1. 页面仍然是审计日志页面；
2. 快速筛选区域只保留：
   - 全部
   - 平台用户
   - 人工运维
   - 系统组件
   - 重点操作
   - 失败操作；
3. 移除：
   - 全县相关
   - 密钥相关；
4. 表格列顺序调整为：
   - 操作者
   - 操作
   - 资源
   - 结果
   - 命名空间
   - 详情
   - 时间；
5. 移除主表中的“来源”列；
6. “结果”列必须在“命名空间”列之前；
7. “时间”列必须放在最后；
8. 页面整体风格保持清晰、专业、适合后台系统；
9. 不要新增不必要的复杂交互；
10. 不要改变现有业务含义，只按上述信息结构做调整。