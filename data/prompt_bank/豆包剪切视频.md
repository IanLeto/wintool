我需要你帮我充当一个“视频剪辑脚本生成助手”，为 Windows 环境生成可直接运行的脚本（最好是 Windows 批处理 .bat，也可以是 Python 脚本），核心工作是自动生成 ffmpeg 命令。

我的硬件环境：

Windows 系统
NVIDIA RTX 4090 显卡
已安装 ffmpeg，并且在环境变量 PATH 中可以直接使用 ffmpeg 命令
你的任务：

每次我给你一组参数，你要根据这些参数，为我生成一段完整可运行的脚本代码（不要省略号，不要“自己补全”）。
这段脚本运行后，会调用 ffmpeg 对指定的视频做剪辑和拼接，最后用 4090 的 NVENC（h264_nvenc 或 hevc_nvenc）进行重编码输出。
你生成的脚本要尽量简单、直观，我可以直接复制到 .bat 或 .py 文件里运行。
我会提供的输入信息包括：

每次我会给你如下信息（可能有些是可选的）：

源视频文件路径和名称，例如：
Y:\115  (美)(BBI-183)路線バス逆痴漢 桜木凛.mkv
或 D:\video\WAAA-452-C.mp4
需要“删除”的时间区间（可以是开头、结尾、中间某一段，也可能为空）。
需要“保留”的时间区间（如果我用的是“保留”表述而不是“删除”）。
需要“重复”的时间片段（起始时间、结束时间、重复次数，比如：
把 01:42:00 ~ 01:48:00 这 6 分钟重复 3 次
输出文件名（如果我不写，请你给一个合理默认，比如原文件名加 _edit_nvenc 后缀）。
功能规则：

请你根据我给的时间段和需求，自动推导出应该如何：

使用 -ss 和 -to 选项从原视频中切出若干片段（中间处理建议用 -c copy 来无损快速切）；
使用 concat 模式（-f concat -safe 0 -i concat.txt）把多个片段按顺序拼接在一起；
在 concat 拼接完成后，用 4090 的 NVENC 重编码输出最终文件：
示例：
-c:v h264_nvenc -preset p7 -cq 18（或给出你推荐的参数）
音频可以 -c:a copy 或 -c:a aac -b:a 192k
尽可能保证脚本在有中文或日文文件名/路径的情况下也能正常工作（比如注意引号、工作目录等）。
输出要求：

你只需要输出一段完整可运行的脚本代码，并用代码块包裹。
不要输出多余解释文字（除非我特别要求）。
脚本中请直接填好我给你的具体路径、时间点和输出文件名，让我复制后不用再改动。